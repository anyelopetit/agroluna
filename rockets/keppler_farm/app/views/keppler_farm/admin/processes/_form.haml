= simple_form_for [:admin, :farm, @process] do |f|
  .container
    .row
      .col-lg-2
        = link_to admin_farm_processes_path, 'data-position': 'bottom', 'data-tooltip': t('keppler-farm.actions.back') do
          .arrow-back.btn.btn-default
            %i.fa.fa-angle-left
            = t('keppler-farm.actions.back')

    .row
      .col-lg-12
        .box.slice-box
          .box-header.with-border
            %h3.box-title= t('keppler-farm.models.pluralize.processes').humanize
            .box-tools.pull-right
              %a.btn.btn-box-tool{'data-widget': 'collapse', type: 'button'}
                %i.fa.fa-minus
          .box-body
            %table.table.table-bordered
              %tbody
                .form-padding
                  .col-xs-12
                    = f.input :title, as: :string
                  .col-xs-12
                    .pull-right
                      = f.button :submit, t('keppler-farm.actions.save'), name: '_save', class: 'btn-primary'
                      = f.button :submit, t('keppler-farm.actions.save_and_add_another'), name: '_add_other'
