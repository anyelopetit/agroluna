menu:
  keppler_farm:
    name: Keppler Farm
    current: ['/']
    url_path: /
    icon: rocket
    model: User
    submenu:
      - farm:
          name: farm
          url_path: /admin/farm/farms
          icon: layers
          current: ['/admin/farm/farms']
          model: KepplerFarm::Farm

      # - new:
      #     name: + Add new module
      #     url_path: /url_path
      #     current: ['/current_path']
      #     enviroment: development
      #     model: User