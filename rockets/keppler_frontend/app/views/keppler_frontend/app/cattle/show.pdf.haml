!!!
%html{ lang: 'en' }
  %head
    %meta{ charset: 'UTF-8' }
    %meta{ content: 'width=device-width, initial-scale=1.0', name: 'viewport' }
    %meta{ name: 'theme-color', content: '#fff' }
    %title Ficha de ganado (#{@cow.serie_number})
    -# = wicked_pdf_stylesheet_link_tag 'https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css'
    -# = wicked_pdf_stylesheet_link_tag '/assets/keppler_frontend/app/application.css'
  %body
    .header-left
      %h1.farm-title= @farm.try(:title)&.upcase
      %h2.report-title Ficha de ganado (#{@cow.serie_number})
    .header-right
      %p= Time.now.strftime('%d-%m-%Y')
    .app_wrapper
      .container_custom
        .row
          %table.card_show.cow_info
            %tbody
              %tr
                %th Número de serie
                %th Nombre corto
                %th Género
                %th Fecha de Nacimiento
              %tr
                %td= @cow.serie_number
                %td= @cow.short_name
                %td= @cow.gender?('male') ? 'Macho' : 'Hembra'
                %td= @cow.birthdate
              %tr
                %th Especie
                %th Tipología
                %th Raza
                %th Condición corporal
              %tr
                %td= @cow.species&.name
                %td= @cow.status&.typology&.name
                %td= @cow.race&.name
                %td= @cow.status&.corporal_condition unless @cow.status&.blank?
              %tr
                %th Peso
                %th Años
                %th Meses
                %th Días de nacido
              %tr
                %td #{@cow.status&.weight} KG
                %td= @cow.years
                %td= @cow.months
                %td= @cow.days
:scss
  body {
    font-size: 12px;
  }
  table {
    border-collapse: collapse;
    width: 100%;
  }
  tr {
    border-top: solid 1px #eee;
  }
  th {
    text-align: left;
    text-transform: uppercase;
    margin: 0;
    padding: 0;
  }
  td {
    margin: 0;
    padding: 0;
  }
  .header-left {
    float: left;
  }
  .header-right {
    float: right;
  }
  .farm-title{
    width: 150%
  }
  .report-title {
    width: 150%
  }